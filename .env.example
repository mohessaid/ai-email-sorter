# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Google OAuth Configuration
# Create OAuth credentials in Google Cloud Console: https://console.cloud.google.com/apis/credentials
# Configure authorized redirect URIs: https://your-project.supabase.co/auth/v1/callback
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# AI Provider Configuration (OpenRouter recommended for free options)
# OpenRouter: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_API_URL=https://openrouter.ai/api

# Alternative: OpenAI (if not using OpenRouter)
# OPENAI_API_KEY=sk-proj-your-key-here

# Optional: Specify AI models
AI_EMBEDDING_MODEL=text-embedding-3-small
AI_LLM_MODEL=gpt-4o-mini

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-generate-with-openssl-rand-base64-32

# Application Configuration
NODE_ENV=development

# Gmail API Configuration
# These are used for server-side Gmail API calls
# The OAuth flow will request these scopes automatically
GMAIL_SCOPES=https://www.googleapis.com/auth/gmail.modify,https://www.googleapis.com/auth/gmail.readonly

# Optional: Redis for background jobs (if implementing worker queue)
# REDIS_URL=redis://localhost:6379

# Optional: Sentry for error tracking
# SENTRY_DSN=your-sentry-dsn

# Optional: Vercel Analytics
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your-analytics-id
